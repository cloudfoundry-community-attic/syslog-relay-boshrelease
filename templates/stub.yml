---
meta:
  environment: NAME
  dns_root: microbosh
  security_groups:
    - syslog_relay
  stemcell:
    name: STEMCELL

director_uuid: PLACEHOLDER-DIRECTOR-UUID

releases:
  - name: syslog-relay
    version: latest
